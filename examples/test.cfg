# Example config file with environment variable expansion
# and directory change support

# Enable debug to see what's happening
debug = true

# Change to /tmp before running
cd = /tmp

# Program to run (with environment variable expansion)
program = ${HOME}/cl/mbasic/com/mbasic.com

# Other settings
default_mode = auto
eol_convert = true
